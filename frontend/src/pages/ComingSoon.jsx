const ComingSoon = ({ title }) => (
  <div>
    <h1 className="page-title">{title}</h1>
    <p className="page-subtitle">
      This module hasn't been built yet — coming next.
    </p>
  </div>
);

export default ComingSoon;
